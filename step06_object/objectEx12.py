import objectEx11

# 아무것도 하지 않고 import만 해도 결과가 나온다! (objectEx11의 내용 실행)
# import는 호출의 의미이므로 objectEx11의 print 내용이 호출됨

# 그러나 objectEx11의 main이 있으면 실행되지 않는다.

# main 있으면 py파일명 자체가 나옴

# main __name__은 실행 파일의 이름을 뜻한다!
# 실행 주체가 되면 __main__ 으로 변경된다.